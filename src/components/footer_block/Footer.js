import { Fragment } from "react";
import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <Fragment>
      <div className={styles["footer-info"]}></div>
    </Fragment>
  );
};

export default Footer;
